# requirements.txt — union dependencies cho 3 folder Vu/, bell/, minhtran/.
#
# Cài 1 lần là chạy được mọi folder:
#     pip install -r requirements.txt
#
# Pin version theo Vu/requirements.txt (đã test thực tế).

# --- Web framework -----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- HTTP clients ------------------------------------------------------------
httpx==0.27.2
requests==2.32.3

# --- LLM client (OpenAI-compatible proxy) ------------------------------------
openai==1.51.0

# --- Embedding & numerics ----------------------------------------------------
sentence-transformers==3.0.1
transformers
torch
huggingface_hub>=0.23.0
numpy==1.26.4

# --- Vector search -----------------------------------------------------------
faiss-cpu==1.8.0          # Vu/ dùng FAISS
chromadb                  # bell/ dùng Chroma
rank-bm25==0.2.2          # Vu/ dùng BM25

# --- LangChain (chỉ bell/ dùng) ---------------------------------------------
langchain
langchain-community
langchain-text-splitters

# --- Misc utilities ----------------------------------------------------------
python-dotenv>=1.0.0
